# Full-product defect ledger

Developer self-review; baseline `416a0d36e6aaf5a431deea80418e4707a2c17c1b`, dirty working tree. Evidence root: `/private/tmp/negosu-full-qa` (local synthetic data, directory mode 700). No production execution.

| ID | Severity | Scope / reproduction and expected result | Root cause / changed files | Regression and status |
| --- | --- | --- | --- | --- |
| QA-001 | HIGH | Seed guard accepted any hosted URL with development flags. Require exact approved target, including port. | `scripts/qa-seed-safety.ts`, seed entry point, runtime env validation, PILOT_QA docs; exact origin allowlist required. | `tests/release-readiness.test.ts`, successful isolated seeding. **VERIFIED_FIXED** |
| QA-002 | HIGH | Existing account matching configured fixture email could have its password reset without fixture ownership. Refuse unrelated accounts. | Main/Pet seed now require administrator-owned `app_metadata.negosu_qa_fixture` before updating existing Auth users. | Account mismatch/ownership regressions in release-readiness tests pass. **VERIFIED_FIXED** |
| QA-003 | MEDIUM | USD organization/service/payment rendered with PHP defaults; appointment payment inserts defaulted to PHP. Preserve record currency and authoritative organization currency for new writes. | Shared/private/public money rendering, catalog/query DTOs, receipt rows, collection grouping; append-only migration `0097_currency_presentation.sql`. New service inserts use server-resolved currency; existing currency is preserved. | Before: four currency SQL failures. After: SQL currency and public estimate checks pass, three currency unit tests pass. Historical grouping unit check and USD service/payment browser pass. Additional hardcoded invoice/job/stock/stay/rate labels, inline service writes and autocomplete currency found and corrected; expanded browser check awaiting rebuilt app. **FIXED_AWAITING_VERIFICATION** |
| QA-004 | LOW | Operational text used semibold/bold and uppercase against requested UI standard. | Shared primitives and consuming pages use regular/medium, normal case; body links have visible underlines. Official logo assets unchanged. | Unit/lint/typecheck/build pass. Seven-viewport screenshot/computed-style review underway. **FIXED_AWAITING_VERIFICATION** |
| QA-005 | HIGH | Dependency audit found vulnerable transitive js-yaml/sharp versions. | `package-lock.json`: updated within existing dependency ranges; no new dependency. | Production and all-dependency npm audits report zero vulnerabilities (`audit-current.json`, `audit-all-current.json`). **VERIFIED_FIXED** |
| QA-006 | LOW | Playwright cleared unrelated QA evidence under its default test-results output directory. | `playwright.config.ts`: scoped outputDir `test-results/playwright`; execution now uses separate per-run protected output paths. | Subsequent runs retain other run outputs. Old baseline logs lost and explicitly documented. **VERIFIED_FIXED** |
| QA-007 | MEDIUM | Mobile iPhone descriptor chose WebKit despite a Chromium executable override. | Explicit Chromium on all mobile projects; exact requested viewport dimensions configured. | Authenticated Pet/Hospitality/walk-in mobile tests executed successfully. **VERIFIED_FIXED** |
| QA-008 | LOW | Existing browser tests assumed hidden selects, counted lookup as mutation, collapsed an already-open reports menu, required an unset email variable, and depended on another suite leaving an unpaid Salon visit. | `e2e/form-actions.spec.ts`, `e2e/core-industry-parity.spec.ts`: assert actual control contract, reuse credentials helper, explicitly seed Salon balance. Assertions retained. | Form suite passed in earlier workflow run; four Core tests now pass in `review-browser.log`. Final repeat pending. **FIXED_AWAITING_VERIFICATION** |
| QA-009 | MEDIUM | `/dashboard/appointments?date=2026-13-01` crashed; impossible calendar dates normalized; agenda assumed 24-hour local days. | `lib/operations.ts` validates dates/roundtrips and derives calendar boundaries; appointments page renders a recoverable error without querying a fabricated date. | Two unit tests cover invalid dates, DST gaps, 23/25-hour days/year rollover. Browser reproduction failed on old build; scoped browser regressions pass in `review-browser-2.log` (both verticals). **VERIFIED_FIXED** |
| QA-010 | LOW | `anon` inherited EXECUTE on appointment payment RPC via PUBLIC. Expected authenticated-only API. Existing in-function role/branch checks still enforced authorization; no unauthenticated write exploit established. | Migration 0097 revokes PUBLIC/anon execution and retains authenticated grant. No RLS/role predicate weakened. | Explicit has_function_privilege and real anon claims regression pass; entire SQL suite passes. **VERIFIED_FIXED** |
| QA-011 | LOW | Streamed dashboard skeleton and loaded content temporarily contain duplicate semantic IDs. | `app/dashboard/loading.tsx` now gives placeholders distinct loading IDs. | Reproduced in Automotive/Hospitality seven-viewport browser audit; four verticals × seven exact viewports passed in `review-browser-2.log`; distinct loading-ID unit check passes. **VERIFIED_FIXED** |

| QA-012 | MEDIUM | Dashboard rounded currency to whole units and called received payments revenue. Preserve decimal precision and label collections accurately. | CommandCenter reuses formatMoney; shared metric and branch cards say Collected today. Internal keys unchanged. | Screenshot inspection and formatter source confirmed; fractional USD unit and real browser readback pass (`access-currency-final.log`, six tests). Actual ready dashboard screenshot inspected: $12.34, Collected today, subtle separators. **VERIFIED_FIXED** |
| QA-013 | LOW | Report daily row separators rendered dark currentColor rather than shared subtle border token. | Reports rows/mobile dividers use border-admin-border. | Visually reproduced in reports-1440.png; final screenshot recheck pending. **FIXED_AWAITING_VERIFICATION** |

| QA-014 | HIGH | Persistent Pet concurrency fixtures copied rollback-only auth.users rows with NULL GoTrue token fields; real Auth listUsers then failed with a NULL-to-string scan error. | Race fixture setup fills empty token fields only on its two synthetic identities; selects an unused fixture namespace to avoid random collisions. Existing malformed disposable rows require scoped local repair. | Six real database races plus atomicity pass; Auth listUsers works afterward and Hospitality signup follows real captured confirmation mail successfully (`pet-concurrency-current.log`, `regression-current.log`). **VERIFIED_FIXED** |

| QA-015 | MEDIUM | Test API clients used global sign-out, invalidating concurrent browser sessions and causing lookup/save failures. | `e2e` API-client cleanup uses scope local; actual product logout unchanged. | Frozen-source `fixed-regression.log`: all searchable appointment/starter scenarios passed after local sign-out change. **VERIFIED_FIXED** |

## Information (not defects)

- Initial SQL CLI command returned NOTESTS, not a pass. Direct psql/TAP runner subsequently executed every SQL suite.
- Local disk exhaustion paused the shared Colima VM with QMP `io-error`. Regenerable Next caches were removed; VM was continued without reset. QA health recovered. Other project sources/data were not modified. Interrupted runs are not clean passes.
- Original `test-results/qa` baseline/build/seed logs were removed by Playwright before outputDir was corrected. Their successful executions are recorded in the session; do not cite nonexistent raw logs.
- Initial duplicate-alert and Salon staff-dialog UI assertions were test targeting errors, not application failures: Next route announcer is outside page content; Salon uses the documented `salon-staff` prefix. Pet dashboard redirects require awaiting the actual destination.
- Production provider setup, hosted smoke, backup/restore and deployment remain unexecuted. External delivery/payment providers are disabled in local QA.
- Complete form/action matrix and all four full browser golden paths remain unfinished; passing suites are not full-product acceptance.
